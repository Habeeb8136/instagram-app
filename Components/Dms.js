import React from 'react';
import {View, StyleSheet ,Text} from 'react-native';

const Dms = () => {
    return (
        <View>
            <Text style={{fontSize:30}}>Dms component</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Dms;
